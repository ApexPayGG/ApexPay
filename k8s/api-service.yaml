apiVersion: v1
kind: Service
metadata:
  name: apexpay-api
  labels:
    app: apexpay-api
spec:
  type: ClusterIP
  selector:
    app: apexpay-api
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
