# Wdrożenie produkcyjne ApexPay: uruchamiane TYLKO po zielonym CI na main (ten sam commit co testy).
# Build obrazu multi-stage → GHCR → SSH + docker compose na serwerze.
name: Deploy production

on:
  workflow_run:
    workflows:
      - CI
    types:
      - completed

concurrency:
  group: deploy-production-${{ github.event.workflow_run.head_sha }}
  cancel-in-progress: false

permissions:
  contents: read
  packages: write
  actions: read

jobs:
  build-push:
    name: Build & Push
    if: >-
      github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.head_branch == 'main'
    runs-on: ubuntu-latest
    outputs:
      image: ${{ steps.meta.outputs.image }}
      web_image: ${{ steps.meta.outputs.web_image }}
    steps:
      - name: Checkout (commit z zakończonego CI)
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: Image name (GHCR, lowercase)
        id: meta
        run: |
          REPO_LOWER=$(echo "${{ github.repository }}" | tr '[:upper:]' '[:lower:]')
          echo "image=ghcr.io/${REPO_LOWER}/apexpay-api:latest" >> "$GITHUB_OUTPUT"
          echo "web_image=ghcr.io/${REPO_LOWER}/apexpay-web:latest" >> "$GITHUB_OUTPUT"

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.image }}

      # VITE_API_URL: puste = względne /api na tym samym hoście (zalecane). Jeśli ustawiasz URL — tylko origin,
      # np. https://apexpay.pl (bez /login). Ścieżka w zmiennej daje błędne .../login/api/v1/... → HTML.
      - name: Build and push web image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile.web
          push: true
          tags: ${{ steps.meta.outputs.web_image }}
          build-args: |
            VITE_API_URL=${{ vars.VITE_API_URL }}

  deploy:
    name: Deploy
    needs: build-push
    runs-on: ubuntu-latest
    steps:
      - name: Checkout (ten sam commit co build — compose zgodny z obrazami)
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: Wgraj docker-compose.prod.yml na VPS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: docker-compose.prod.yml
          target: /opt/apexpay-api/

      - name: Deploy over SSH
        uses: appleboy/ssh-action@master
        env:
          GHCR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GHCR_USERNAME: ${{ github.actor }}
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          envs: GHCR_TOKEN,GHCR_USERNAME
          script: |
            set -euo pipefail
            cd /opt/apexpay-api
            REPO_LOWER=$(echo "${{ github.repository }}" | tr '[:upper:]' '[:lower:]')
            export APEXPAY_API_IMAGE="ghcr.io/${REPO_LOWER}/apexpay-api:latest"
            export APEXPAY_WEB_IMAGE="ghcr.io/${REPO_LOWER}/apexpay-web:latest"
            echo "$GHCR_TOKEN" | docker login ghcr.io -u "$GHCR_USERNAME" --password-stdin
            COMPOSE_CMD="docker compose -f docker-compose.prod.yml"
            if [ -f .env.prod ]; then
              COMPOSE_CMD="$COMPOSE_CMD --env-file .env.prod"
            fi

            # Pobierz obrazy dla wszystkich serwisów z pliku compose na serwerze (bez listy nazw —
            # stary compose bez `web` nie wywali się na `pull ... web`).
            $COMPOSE_CMD pull

            set +e
            $COMPOSE_CMD up -d
            UP_EXIT=$?
            set -e

            if [ "$UP_EXIT" -ne 0 ]; then
              echo "[deploy] docker compose up failed, printing diagnostics..."
              $COMPOSE_CMD ps || true
              docker logs --tail 200 apexpay-prod-api-migrator-1 || true
              docker logs --tail 120 apexpay-prod-postgres-1 || true
              exit "$UP_EXIT"
            fi

  notify:
    name: Notify
    runs-on: ubuntu-latest
    needs: [build-push, deploy]
    if: >-
      always() &&
      github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.head_branch == 'main'
    env:
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
    steps:
      - name: Slack — raport wdrożenia
        if: env.SLACK_WEBHOOK_URL != ''
        uses: rtCamp/action-slack-notify@v2
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK_URL }}
          SLACK_COLOR: ${{ (contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled')) && '#ff0000' || '#36a64f' }}
          SLACK_TITLE: Raport z wdrożenia ApexPay
          SLACK_MESSAGE: "Etap build-push: ${{ needs.build-push.result }} | Etap deploy: ${{ needs.deploy.result }} | Commit: ${{ github.server_url }}/${{ github.repository }}/commit/${{ github.event.workflow_run.head_sha }} | CI: ${{ github.event.workflow_run.html_url }}"
