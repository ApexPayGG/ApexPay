/**
 * CI: podpisuje testowy JWT (payload.userId) — używany jako STRESS_JWT.
 * Wymaga JWT_SECRET w środowisku. Na stdout tylko token (jedna linia).
 */
import "dotenv/config";
//# sourceMappingURL=generate-ci-jwt.d.ts.map